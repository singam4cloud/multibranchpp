pipeline {
    agent any
    stages {
        stage('---Build---') {
            steps {
                sh '''#!/bin/bash
                echo "Hello World!"
                '''
            }
        }
    }
}
